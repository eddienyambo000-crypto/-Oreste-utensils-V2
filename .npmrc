# Vitest 5 lists an optional peer on @types/node >= 22 while the app pins
# @types/node 20 (matched to the Node 20 runtime). The conflict is harmless
# (types only), so tolerate it — otherwise Vercel's `npm install` fails the build.
legacy-peer-deps=true
